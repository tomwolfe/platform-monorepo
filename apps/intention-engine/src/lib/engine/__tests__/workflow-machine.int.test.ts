/**
 * Workflow Machine Integration Tests
 *
 * SKIPPED - Requires real Redis backend.
 * This is an integration test that needs external infrastructure.
 *
 * TODO: Refactor into proper integration test suite with Redis setup.
 */

import { describe, it } from "vitest";

describe.skip("Workflow Machine Integration - requires Redis", () => {
  it("is skipped", () => {});
});
