/**
 * Mock for 'server-only' package
 *
 * This package is used to prevent server-only code from being included
 * in client components. In tests, we mock it to avoid import errors.
 */
export default {};
