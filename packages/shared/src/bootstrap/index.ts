/**
 * Bootstrap Utilities - Startup Validation
 *
 * Fail-fast validation for application startup.
 * Prevents runtime crashes from missing environment variables.
 *
 * @package @repo/shared
 */

export { bootstrapEnv, validateEnvSubset } from "./validate-env";
